#include<iostream>
#include<vector>
using namespace std;

int main(){
    int m, n;
    cout << "Enter value of m and n : ";
    cin >> n >> m;
    vector<vector<int>> vec(n,vector<int>(m));
    return 0;
}